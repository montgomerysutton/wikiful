<% # Categories Index View: categories/index.html.erb %>

<h1>This is the Index View for Categories</h1>

<% @categories.each do |category| %>
	<div>
		<h3><%= link_to category.name, category %>
			(<%= category.articles.count %> Articles)
		</h3>
	</div>
<% end %>